# services

Business-logic services that sit between controllers and models, plus socket
`emitters/` and `listeners/`. Mirrors the layout of the Recibia server.

Naming convention: `*.service.js` (e.g. `transaction.service.js`).
